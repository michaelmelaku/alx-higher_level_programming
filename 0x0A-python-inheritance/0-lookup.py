#!/usr/bin/python3
"""dirs the object passed to it"""


def lookup(obj):
    """ looks up attributes

    Args:
        obj: the object to dir

    Returns:
    list: the dir info of the obj

    """
    return dir(obj)
